{
  "name": "flamingo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:dev": "NODE_ENV=test nodemon src/server",
    "start": "node src/server",
    "migration:make": "NODE_ENV=test knex --knexfile src/database/knexfile.js migrate:make",
    "migration:latest": "NODE_ENV=test knex --knexfile src/database/knexfile.js migrate:latest",
    "migration:rollback": "NODE_ENV=test knex --knexfile src/database/knexfile.js migrate:rollback",
    "migration:run": "NODE_ENV=test knex --knexfile src/database/knexfile.js migrate:up",
    "seed:make": "NODE_ENV=test knex --knexfile src/database/knexfile.js seed:make",
    "seed:run": "NODE_ENV=test knex --knexfile src/database/knexfile.js seed:run"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "jsonwebtoken": "^8.5.1",
    "knex": "^0.95.4",
    "multer": "^1.4.2",
    "path": "^0.12.7",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
